---
setup_ssh: true

setup_password_lock: true
setup_root_password_lock: true
setup_ssh_key_file: '~/.ssh/id_ed25519.pub'
setup_sole_authorized_key: true

setup_install_packages: true

setup_additional_packages:
  - ncurses-term

setup_sudo: true

setup_user: "{{ user }}"
